import 'package:flutter/material.dart';

class CompetitorData extends StatelessWidget {
  const CompetitorData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
